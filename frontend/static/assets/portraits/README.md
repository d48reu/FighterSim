# Fighter portrait library

Style: broadcast-illustrated-v1

This is the starter portrait library for FighterSim.

Conventions:
- deterministic assignment uses `age/style/global` buckets
- portraits are shoulders-up broadcast-card SVGs
- keep framing, lighting family, and background treatment consistent
- add new files to `manifest.json` explicitly; do not rely on directory scans
